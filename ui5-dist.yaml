specVersion: "4.0"
metadata:
  name: siagrob1
type: application
resources:
  configuration:
    paths:
      webapp: dist
framework:
  name: SAPUI5
  version: "1.141.0"
  libraries:
    - name: sap.m
    - name: sap.ui.core
    - name: themelib_sap_horizon
    # O usuário escolhe o tema no perfil: sem esta lib, sap_fiori_3 (o padrão do bootstrap) fica
    # sem CSS no build otimizado.
    - name: themelib_sap_fiori_3
